import React from "react";
const PageTitle = ({title}) => {
  return (
    <h1 className="page-title d-block w-fit-content text-uppercase m-0 m-auto">
          {title}
    </h1>
  );
};

export default PageTitle;
